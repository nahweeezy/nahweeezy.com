# Local environment
.env
.env.local
.vercel
.DS_Store
*.log

# Build artifacts & caches
node_modules
dist
.vite

# Per-machine Claude Code preferences (paths, allowlist) — keep launch.json tracked, exclude local user settings.
.claude/settings.local.json
